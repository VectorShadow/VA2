package combat.melee;

import combat.CombatResolver;

/**
 * CombatResolver for Melee combat interactions.
 */
public class MeleeResolver extends CombatResolver {
    //todo - when an attacker initiates an attack, select a tactic from its continuum(or use the player's selection).

    //todo - when an attacker resolves an attack, select a defense tactic. This remains in effect until the next attack.
}
